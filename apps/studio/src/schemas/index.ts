import {
  baseStyles,
  footer,
  homepage,
  page,
  redirect,
  searchPage,
  siteSettings,
} from "@documents";
import { meta } from "@objects";

export const schemaTypes = [
  // Documents
  homepage,
  searchPage,
  footer,
  page,
  siteSettings,
  baseStyles,
  redirect,
  // Objects
  meta,
];
