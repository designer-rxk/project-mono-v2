import { Typography } from "@mono/ui";

export default function Home() {
  return (
    <div>
      <Typography
        as="h1"
        className="w-full justify-center flex h-screen items-center"
      >
        Mono Web workspace
      </Typography>
    </div>
  );
}
